console.log("\n=== MEU PRIMEIRO PROGRAMA ===")

// Entrada (Dados)
let nome = prompt("Insira seu nome:");

// Processamento (Dados)
nome = nome.toUpperCase();

// Saida (Dados)
document.write(nome);
